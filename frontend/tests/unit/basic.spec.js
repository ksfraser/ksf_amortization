import { describe, it, expect } from 'vitest'

describe('Pure math test', () => {
  it('1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2)
  })
  
  it('multiply', () => {
    expect(5 * 3).toBe(15)
  })
})
